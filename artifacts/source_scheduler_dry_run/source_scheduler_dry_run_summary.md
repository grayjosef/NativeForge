# Source scheduler dry run

Generated by `source_scheduler_dry_run_artifact_service`. This is a list of work that *would* be done. Nothing consumes it, nothing dispatched, and no request was made.

## Declarations

```text
dry_run_only              true
live_jobs_created         0
collectors_executed       false
live_fetch_performed      false
raw_payloads_written      false
source_monitoring_live    false
live_source_coverage      false
runtime_mode              dry_run_in_process
```

## Runtime

| Fact | Value |
| --- | --- |
| `runtime_mode` | dry_run_in_process |
| `scheduler_runtime_available` | true |
| `background_worker_available` | false |
| `source_monitoring_live` | false |
| `ready_to_start_monitoring` | false |

`scheduler_runtime_available` is true because `runtime_mode` is `dry_run_in_process` - an in-process queue builder that executes nothing. It is not a background worker and it is not monitoring. The remaining work is `background_worker`, `periodic_trigger`, `persistent_backend`, `scheduler_runtime`.

## Queue

```text
decisions considered   5
not queueable          0
jobs_total             5
jobs_queued            0
jobs_blocked           5
jobs_deduplicated      0
live_jobs_created      0
```

| Source | Status | Mode | Blocked by |
| --- | --- | --- | --- |
| `sam_assistance_listings_api` | blocked | dry_run | `activation_not_allowed:activation_blocked`, `circuit_does_not_permit:unknown`, `production_payload_store_unavailable:unavailable`, `schedule_does_not_permit_enqueue:unknown` |
| `federal_register_api` | blocked | dry_run | `activation_not_allowed:activation_blocked`, `circuit_does_not_permit:unknown`, `production_payload_store_unavailable:unavailable`, `schedule_does_not_permit_enqueue:unknown` |
| `usaspending_api_v2` | blocked | dry_run | `activation_not_allowed:activation_blocked`, `circuit_does_not_permit:unknown`, `production_payload_store_unavailable:unavailable`, `schedule_does_not_permit_enqueue:unknown` |
| `grants_gov_search2_fetch` | blocked | dry_run | `activation_not_allowed:activation_blocked`, `circuit_does_not_permit:unknown`, `production_payload_store_unavailable:unavailable`, `schedule_does_not_permit_enqueue:unknown` |
| `grants_gov_daily_extract` | blocked | dry_run | `activation_not_allowed:activation_blocked`, `circuit_does_not_permit:unknown`, `production_payload_store_unavailable:unavailable`, `schedule_does_not_permit_enqueue:unknown` |

## Why every job is blocked

No Phase 1 source has ever been checked, so none has a `next_check_due_at`, and Gate 98B reports `unknown` rather than treating an absent schedule as a licence to run now. On top of that, no production raw payload store exists, so a check would have nowhere durable to put what came back.

Blocked jobs are kept in the queue rather than dropped. A queue that listed only the workable sources would look healthy by omission.

