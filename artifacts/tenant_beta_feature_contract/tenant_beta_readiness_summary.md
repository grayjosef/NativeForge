# Tenant beta readiness

Generated by `tenant_beta_contract_artifact_service`. No collector ran, no source was checked, no message was sent, and no tenant fact was invented.

## Declarations

```text
tenant_beta_contract_available      true
ready_for_demo_contract             true
ready_for_beta_onboarding           false
live_source_collection_available    false
email_delivery_available            false
source_monitoring_live              false
live_source_coverage                false
demo_scope                          contract_demo_against_demo_safe_fixtures
```

`ready_for_demo_contract` is true and narrow: it means the **contract demo against demo-safe fixtures**. It is not a claim that live matching, live digests, or real tenant data work, because none of those exists.

## What is missing before a tenant could be onboarded

- `customer_auth_live`
- `customer_persistence_live`
- `email_delivery_available`
- `live_source_collection_available`

## Demo tenants

4 generic demo tenants. No real Tribe is named. 0 facts are verified, 10 are demo fixtures, and 14 are unknown.

Recognition status, applicant classes and service area are `unknown` for every demo tenant, deliberately. Fabricating them is the specific harm the fixture service exists to avoid, and a demo that shows `unknown` is showing how the product behaves when it does not know — which is most of the time.

| Tenant | Kind | Operating states | SC priority | Fact status |
| --- | --- | --- | --- | --- |
| `nf-demo-tenant-01` | tribal_government | SC | true | unknown |
| `nf-demo-tenant-02` | tribal_government | SC | true | unknown |
| `nf-demo-tenant-03` | native_nonprofit | NC | false | unknown |
| `nf-demo-tenant-04` | unknown | — | false | unknown |

## Source priority

| Tenant | SC | Federal | Active | Monitored |
| --- | --- | --- | --- | --- |
| `nf-demo-tenant-01` | 57 | 303 | 0 | 0 |
| `nf-demo-tenant-02` | 57 | 303 | 0 | 0 |
| `nf-demo-tenant-03` | 0 | 303 | 0 | 0 |
| `nf-demo-tenant-04` | 0 | 303 | 0 | 0 |

South Carolina priority is **tenant-specific**. The two SC demo tenants get 57 SC sources ranked first; the tenant operating in another state gets none, and the tenant with no operating state gets none. Every tier has zero active collectors.

## Allowability review

Evidence-backed only: no affirmative label without a quote or reference from the opportunity's own text. **When the assessed cost is NativeForge itself, the label is capped at `requires_human_review` regardless of evidence strength** — a tool telling a customer that buying the tool is grant-allowable has an obvious incentive problem, and a self-assessment that can only ever say "ask a human" is the defensible version.

## What must happen next

1. `build_digest_contract` — gate 104 - digest is genuine greenfield; nothing backs it today
2. `build_pursuit_suppression_contract` — gate 104 - tenant-specific, never global, and never a delete
3. `extend_awarded_grants_requirement_tracking` — gate 105 - separate projected burden from active obligations
4. `prove_customer_persistence_for_tenant_profiles` — no tenant profile persists today; NfTribalProfile carries none of the beta fields
5. `prove_customer_auth` — login is not live; onboarding a paying tenant needs it
6. `prove_email_delivery` — a weekly digest nobody can receive is not a weekly digest
7. `activate_a_collector_under_the_existing_gates` — live source collection is 0; every tenant feature is fixture-backed until it is not

