{
  "email_carried_in_payload": false,
  "email_omission_rationale": "an email in a cookie is personal data travelling on every request to every route, readable by anything that can see the cookie jar. The subject and the organization are what a route needs; the email is looked up when it is actually wanted",
  "expiry_carried_in_payload": true,
  "expiry_rationale": "a cookie Max-Age is a request to the browser. A browser that ignores it, or an attacker replaying a captured cookie, is unaffected - so the value carries its own expiry and the verifier checks it server-side",
  "fabricated": false,
  "format_version": "nf1",
  "max_session_seconds": 604800,
  "organization_id_must_be_uuid": true,
  "payload_fields": [
    "v",
    "sid",
    "pid",
    "sub",
    "org",
    "roles",
    "iat",
    "exp",
    "src",
    "email_omitted"
  ],
  "production_sessions_created": false,
  "real_users_created": false,
  "revocation_note": "a signed session needs no storage to verify, and cannot be revoked before it expires. Logout clears the cookie; it cannot un-sign a value already issued. That is why the lifetime is bounded and rotation is required",
  "revocation_supported": false,
  "schema_version": "nf_customer_session_state_artifact_v1",
  "session_cookie_value_included": false,
  "session_format_contract_available": true,
  "shape": "<version>.<base64url(payload_json)>.<base64url(hmac_sha256)>",
  "signature_algorithm": "HMAC-SHA256",
  "signing_key_env": "NF_SESSION_SIGNING_KEY",
  "signing_key_present": false,
  "signing_key_value_included": false
}
