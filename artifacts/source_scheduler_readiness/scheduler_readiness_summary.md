# Source scheduler readiness

Generated by `source_scheduler_readiness_artifact_service`. Every value below is detected by importing or inspecting the thing itself, not read from a declaration.

## Declarations

```text
runtime_mode                    dry_run_in_process
scheduler_runtime_available     true
background_worker_available     false
source_monitoring_live          false
ready_to_start_monitoring       false
```

`scheduler_runtime_available` is true because `runtime_mode` is `dry_run_in_process` - an in-process queue builder that executes nothing. It is not a background worker, and it is not monitoring. `scheduler_package_installed` is false.

## Components

| Component | Kind | Present | Detected by |
| --- | --- | --- | --- |
| `schedule_decision_service` | contract | yes | import + callable check |
| `circuit_breaker_service` | contract | yes | import + callable check |
| `check_run_contract_service` | contract | yes | import + callable check |
| `production_raw_payload_store` | contract | no | gate 96/97 readiness derivation |
| `dry_run_runtime` | runtime | yes | import + callable check |
| `dry_run_worker` | runtime | yes | import + callable check |
| `scheduler_runtime` | runtime | no | importlib.util.find_spec |
| `background_worker` | runtime | no | find_spec + console_scripts entry points |
| `periodic_trigger` | runtime | no | repo file scan |

## What is missing

- `background_worker`
- `periodic_trigger`
- `production_raw_payload_store`
- `scheduler_runtime`

The decision layer is present and the runtime is not. Having the services a scheduler would consult is not having a scheduler, in the same way that Gate 95's payload contract was not a payload store.

## Circuit breaker

Threshold 5 consecutive failures, cooldown 3600s. A cooldown that elapses produces `half_open`, which permits one probe - not a resumption at full rate.

| Scenario | Status | Permits scheduling |
| --- | --- | --- |
| closed | `closed` | yes |
| closed_below_threshold | `closed` | yes |
| open_cooldown_running | `open` | no |
| half_open_cooldown_elapsed | `half_open` | yes |
| open_cooldown_not_derivable | `open` | no |
| manual_hold | `manual_hold` | no |
| unknown_failure_count_absent | `unknown` | no |
| unknown_override_unrecognised | `unknown` | no |

## Schedule decisions

| Scenario | Status | Due | Safe to enqueue |
| --- | --- | --- | --- |
| nothing_supplied | `unknown` | no | no |
| due_and_cleared | `due_and_safe_to_enqueue` | yes | yes |
| not_yet_due | `not_due` | no | no |
| no_schedule_recorded | `unknown` | no | no |
| monitoring_disabled | `disabled` | yes | no |
| terms_review_required | `due_but_blocked` | yes | no |
| human_review_only | `due_but_blocked` | yes | no |
| circuit_open | `due_but_blocked` | yes | no |
| no_production_payload_store | `due_but_blocked` | yes | no |

`safe_to_execute_now` is false on every decision in this gate: nothing executes, so nothing may report that executing is safe.

## Check-run contract

20 fields for `nf_source_check_runs`. Payloads are referenced by id, never by content, and the error message is redacted to `[REDACTED]` on the way in rather than trusted to have been redacted already.

