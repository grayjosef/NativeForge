# Tenant NOFO digest readiness

Generated by `tenant_nofo_digest_artifact_service`. No email was sent, no collector ran, no source was checked, and nothing was deleted.

## Declarations

```text
digest_contract_available           true
weekly_digest_preview_available     true
ready_for_demo_preview              true
ready_for_operational_digest        false
email_delivery_available            false
source_monitoring_live              false
live_source_collection_available    false
live_source_coverage                false
customer_persistence_live           false
demo_scope                          digest_preview_from_labelled_fixture_snapshots
comparison_kind                     fixture_to_fixture
```

Three lines are true and all three describe a **preview**: the contracts exist and a digest can be rendered from labelled fixture snapshots. None of them says a digest arrives anywhere.

## The preview compares two recorded snapshots

`comparison_kind` is `fixture_to_fixture`. Change detection needs two observations and there is no live collection to produce a second one, so the preview compares a recorded pair and labels the result rather than presenting it as a week of monitoring.

## Digest preview

```text
cadence                             weekly
delivery_status                     preview_only
items_total                         10
items_visible                       8
items_suppressed                    1
items_human_review                  3
items_with_unverified_deadlines     5
items_with_unknown_reporting_burden 5
```

5 of 10 items carry a deadline nobody can vouch for, and that count is in the header rather than buried per item. A digest where most deadlines are unverified should say so at the top.

| Opportunity | Status | Visible | Review | Deadline verified |
| --- | --- | --- | --- | --- |
| `opp-amended` | changed | true | false | true |
| `opp-approaching` | new_match | true | false | true |
| `opp-deadline-unverif` | changed | true | true | false |
| `opp-deadline-verified` | changed | true | false | true |
| `opp-downgraded` | downgraded | true | false | false |
| `opp-excluded` | newly_excluded | true | false | false |
| `opp-new-match` | new_match | true | false | true |
| `opp-removed` | high_fit_unreviewed | false | true | false |
| `opp-review` | needs_human_review | true | true | false |
| `opp-suppressed` | suppressed | false | false | true |

## Suppression

1 item suppressed, **counted not deleted**. Source history and provenance are preserved, the opportunity stays visible in the tenant's pursuit pipeline, and no other tenant's digest is affected.

## What is missing before an operational digest

- `customer_persistence_live`
- `email_delivery_available`
- `live_source_collection_available`

## What must happen next

1. `build_email_delivery` — no email service exists; a weekly digest nobody receives is not a weekly digest
2. `persist_tenant_digests` — no digest table exists; a digest that cannot be re-read cannot be audited after a missed deadline
3. `activate_a_collector_under_the_existing_gates` — change detection compares two recorded snapshots today; a live comparison needs a second real observation
4. `settle_the_pursuit_pipeline_vocabulary` — three vocabularies disagree - PursuitWorkflowStatus, pursuit_workspace_contract, and doc 570's seven stages
5. `extend_awarded_grants_requirement_tracking` — gate 105 - separate projected burden from active obligations

